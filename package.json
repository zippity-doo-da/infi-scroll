{
  "name": "infi-scroll",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "soak": "node scripts/soak-runtime.mjs",
    "pack:new": "node scripts/world-pack.mjs new",
    "pack:validate": "node scripts/world-pack.mjs validate",
    "pack:install": "node scripts/world-pack.mjs install"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "phaser": "^3.90.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/three": "^0.185.4",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
